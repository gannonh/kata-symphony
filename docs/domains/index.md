# Domains

Product and domain vocabulary for this monorepo.

This repo uses a **single-context** domain docs layout. See [Domain docs layout](../agents/domain.md).

# Current sources

* Root `CONTEXT.md` — glossary and ubiquitous language (created lazily by `/domain-modeling` when terms are resolved; may not exist yet)
* [ADRs](../adrs/) — durable architecture decisions that constrain the domain

# Concepts

_No domain concept documents under `domains/` yet. Prefer defining terms in `CONTEXT.md` and linking here when a concept needs a longer note._
