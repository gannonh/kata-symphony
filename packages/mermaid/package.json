{
  "name": "@kata/mermaid",
  "version": "0.6.0",
  "license": "MIT",
  "description": "Mermaid flowchart to styled SVG renderer with zinc monochrome palette",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "samples": "node --import tsx index.ts",
    "dev": "node --import tsx dev.ts",
    "bench": "bun run bench.ts",
    "build": "node --import tsx index.ts && mkdir -p dist && mv index.html dist/ && cp -r public/* dist/",
    "deploy": "bun run build && wrangler pages deploy dist --project-name craft-agents-mermaid",
    "lint": "eslint src/ --max-warnings=50",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dagrejs/dagre": "^3.1.0"
  },
  "devDependencies": {
    "chokidar": "^5.0.0",
    "esbuild": "^0.28.1",
    "shiki": "^4.4.2",
    "tsx": "^4.23.1",
    "vitest": "^4.1.10"
  }
}
